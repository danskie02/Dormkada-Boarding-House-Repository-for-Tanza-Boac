# API Server URL (without /api suffix - it's added automatically in App.tsx)
# For local development with API server on port 3001:
VITE_API_URL=http://localhost:3001
# For production, set to your deployed API server root URL WITHOUT trailing slash:
# VITE_API_URL=https://your-api-server.onrender.com
